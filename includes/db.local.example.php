<?php
/**
 * Legacy placeholder — database credentials live only in the project root `.env` file.
 * Do not add `includes/db.local.php`; it is not loaded by this application.
 *
 * Copy `.env.example` to `.env` and set DB_* there.
 */
